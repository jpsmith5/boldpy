# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.4.0
scikit-image>=0.18.0
scikit-learn>=1.0.0
Pillow>=8.0.0
tqdm>=4.60.0
PyYAML>=5.1

# DICOM support (required for prepare_dicom.py)
pydicom>=2.3.0

# PEP ecosystem (sample metadata, pipeline submission, result reporting)
peppy>=0.40.0
eido>=0.2.0
looper>=2.1.0
piper>=0.15.0       # pypiper: pipeline execution + checkpoints (PyPI name: piper)
pipestat>=0.12.0
